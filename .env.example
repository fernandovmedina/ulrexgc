# local is the default and needs no infrastructure. Set supabase only after a project exists.
NEXT_PUBLIC_DATA_DRIVER=local

# Leave both values empty until a real Supabase project exists.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
