"use client";

import Image from "next/image";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useEffect, useState } from "react";
import { ArrowLeft, Eye, EyeOff, LockKeyhole } from "lucide-react";
import { useSubmitLock } from "@/components/admin/use-submit-lock";
import { authRepository, LOCAL_ADMIN_EMAIL, LOCAL_ADMIN_PASSWORD } from "@/lib/auth/repository";

export default function LoginPage() {
  const router = useRouter();
  const [email, setEmail] = useState(LOCAL_ADMIN_EMAIL);
  const [password, setPassword] = useState(LOCAL_ADMIN_PASSWORD);
  const [show, setShow] = useState(false);
  const [error, setError] = useState("");
  const { saving: loading, lock, unlock } = useSubmitLock();
  useEffect(() => { void authRepository.getSession().then((session) => { if (session) router.replace("/admin"); else if (new URLSearchParams(window.location.search).get("error") === "not-admin") setError("This account is not an administrator."); }).catch(() => setError("Your session could not be read. Please sign in again.")); }, [router]);
  const submit = async (event: React.FormEvent) => { event.preventDefault(); if (!lock()) return; setError(""); try { await authRepository.signIn({ email, password }); router.replace("/admin"); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to sign in."); } finally { unlock(); } };
  return <main className="grid min-h-screen bg-[#061426] px-5 py-10 text-white lg:grid-cols-2 lg:p-0"><section className="hidden blueprint-grid items-end p-14 lg:flex"><div className="max-w-lg"><p className="font-mono text-[10px] uppercase tracking-[.25em] text-[#e2be6c]">Ulrex operations</p><h1 className="mt-5 text-6xl font-black uppercase leading-[.9] tracking-[-.055em]">Clarity for every lead.</h1><p className="mt-6 leading-7 text-white/55">A focused workspace for intake, follow-up, and the work that moves projects forward.</p></div></section><section className="flex items-center justify-center"><div className="w-full max-w-md"><Link href="/" className="mb-10 inline-flex items-center gap-2 text-sm text-white/55 hover:text-white"><ArrowLeft className="size-4" />Back to website</Link><div className="border border-white/10 bg-white/[.04] p-7 shadow-2xl sm:p-10"><Image src="/logo_icon_with_transparent_background.png" alt="Ulrex General Contracting" width={54} height={54} className="mb-7 size-12 object-contain" /><p className="font-mono text-[9px] uppercase tracking-[.24em] text-[#e2be6c]">Admin access</p><h2 className="mt-3 text-3xl font-black uppercase tracking-[-.035em]">Sign in</h2><p className="mt-2 text-sm leading-6 text-white/45">Use your administrator account to open the CRM.</p><form onSubmit={submit} className="mt-8 space-y-5"><div><label htmlFor="email" className="mb-2 block text-xs font-bold">Email</label><input id="email" type="email" autoComplete="username" required value={email} onChange={(event) => setEmail(event.target.value)} className="h-12 w-full border border-white/15 bg-[#030d17] px-4 text-sm outline-none focus:border-[#d6aa55]" /></div><div><label htmlFor="password" className="mb-2 block text-xs font-bold">Password</label><div className="relative"><input id="password" type={show ? "text" : "password"} autoComplete="current-password" required value={password} onChange={(event) => setPassword(event.target.value)} className="h-12 w-full border border-white/15 bg-[#030d17] px-4 pr-12 text-sm outline-none focus:border-[#d6aa55]" /><button type="button" onClick={() => setShow((value) => !value)} aria-label={show ? "Hide password" : "Show password"} className="absolute right-0 top-0 grid size-12 place-items-center text-white/50">{show ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></div></div>{error && <p role="alert" className="text-sm text-red-300">{error}</p>}<button disabled={loading} className="flex h-12 w-full items-center justify-center gap-2 bg-[#d6aa55] font-mono text-[10px] font-bold uppercase tracking-[.18em] text-[#061426] transition hover:bg-[#e2be6c] disabled:opacity-50"><LockKeyhole className="size-4" />{loading ? "Signing in…" : "Sign in"}</button></form>{process.env.NEXT_PUBLIC_DATA_DRIVER !== "supabase" && <p className="mt-6 border-t border-white/10 pt-5 text-xs leading-5 text-white/40">Local demo credentials are prefilled. This development mode does not provide production security.</p>}</div></div></section></main>;
}
